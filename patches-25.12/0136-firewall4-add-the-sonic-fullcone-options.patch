From 232931f87478047394f5e6787045ba4e3a906d9a Mon Sep 17 00:00:00 2001
From: leerep <leerep@163.com>
Date: Thu, 24 Sep 2026 12:00:03 +0800
Subject: [PATCH] firewall4: add the sonic fullcone options

Add the same three-level control to firewall4 (defaults.fullcone, per-zone
fullcone, per-zone fullcone_proto) and emit the fullcone statement on the SNAT
rules.  Every protocol with a port range gets one, because the 3-tuple
uniqueness check of the fullcone NAT core only runs in its port-range branch.

Depends on the libnftnl/nftables fullcone expression patches and on the kernel
side shipped by the qca-wifi-7 feed.

Signed-off-by: leerep <leerep@163.com>
---
 .../patches/001-sonic-fullcone.patch          | 262 ++++++++++++++++++
 1 file changed, 262 insertions(+)
 create mode 100644 package/network/config/firewall4/patches/001-sonic-fullcone.patch

diff --git a/package/network/config/firewall4/patches/001-sonic-fullcone.patch b/package/network/config/firewall4/patches/001-sonic-fullcone.patch
new file mode 100644
index 0000000000..944638d4d6
--- /dev/null
+++ b/package/network/config/firewall4/patches/001-sonic-fullcone.patch
@@ -0,0 +1,262 @@
+From: openwrt-sonic-fullcone contributors
+Date: Wed, 9 Apr 2026 00:00:00 +0000
+Subject: [PATCH] firewall4: add SONiC fullcone NAT with per-zone and
+ per-protocol granularity
+
+Adds fullcone NAT support to firewall4 with three levels of control:
+
+  1. Global default:  config defaults / option fullcone 1
+  2. Per-zone:        config zone / option fullcone 1
+  3. Per-protocol:    config zone / list fullcone_proto udp
+
+defaults.fullcone is a global gate. When off, all fullcone is disabled.
+When on, each zone must explicitly set fullcone=1 to activate.
+
+Requires kernel module nft_fullcone (from 985-add-sonic-fullcone-to-nft
+patch) and the corresponding libnftnl/nftables userspace patches.
+
+SNAT rules carry a port range for every protocol that has one - ports,
+the icmp id, the gre key. SONiC's get_unique_tuple() checks 3-tuple
+uniqueness only in its NF_NAT_RANGE_PROTO_SPECIFIED branch, and SONiC's
+own natmgr never emits --fullcone without a range; without one, two
+inside hosts can be given the same external port. DNAT rules carry no
+range, as in SONiC.
+
+---
+ root/usr/share/firewall4/templates/ruleset.uc      |   48 +++++++++++++
+ .../usr/share/firewall4/templates/zone-fullcone.uc |   33 +++++++++
+ root/usr/share/ucode/fw4.uc                        |   72 +++++++++++++++++++-
+ 3 files changed, 149 insertions(+), 4 deletions(-)
+ create root/usr/share/firewall4/templates/zone-fullcone.uc
+
+--- a/root/usr/share/firewall4/templates/ruleset.uc
++++ b/root/usr/share/firewall4/templates/ruleset.uc
+@@ -327,6 +327,24 @@ table inet fw4 {
+ {%   for (let redirect in fw4.redirects(`dstnat_${zone.name}`)): %}
+ 		{%+ include("redirect.uc", { fw4, zone, redirect }) %}
+ {%   endfor %}
++{%   if (zone.masq && zone.fullcone): %}
++{%    if (zone.fullcone_proto && length(zone.fullcone_proto)): %}
++{%     for (let proto in zone.fullcone_proto): %}
++		{%+ include("zone-fullcone.uc", { fw4, zone, family: 4, direction: "dstnat", proto }) %}
++{%     endfor %}
++{%    else %}
++		{%+ include("zone-fullcone.uc", { fw4, zone, family: 4, direction: "dstnat", proto: null }) %}
++{%    endif %}
++{%   endif %}
++{%   if (zone.masq6 && zone.fullcone6): %}
++{%    if (zone.fullcone_proto && length(zone.fullcone_proto)): %}
++{%     for (let proto in zone.fullcone_proto): %}
++		{%+ include("zone-fullcone.uc", { fw4, zone, family: 6, direction: "dstnat", proto }) %}
++{%     endfor %}
++{%    else %}
++		{%+ include("zone-fullcone.uc", { fw4, zone, family: 6, direction: "dstnat", proto: null }) %}
++{%    endif %}
++{%   endif %}
+ {%   fw4.includes('chain-append', `dstnat_${zone.name}`) %}
+ 	}
+ 
+@@ -337,14 +355,40 @@ table inet fw4 {
+ {%   for (let redirect in fw4.redirects(`srcnat_${zone.name}`)): %}
+ 		{%+ include("redirect.uc", { fw4, zone, redirect }) %}
+ {%   endfor %}
+-{%   if (zone.masq): %}
++{%   if (zone.masq && zone.fullcone): %}
++{%    if (zone.fullcone_proto && length(zone.fullcone_proto)): %}
++{%     for (let proto in zone.fullcone_proto): %}
++		{%+ include("zone-fullcone.uc", { fw4, zone, family: 4, direction: "srcnat", proto }) %}
++{%     endfor %}
++{%     for (let saddrs in zone.masq4_src_subnets): %}
++{%      for (let daddrs in zone.masq4_dest_subnets): %}
++		{%+ include("zone-masq.uc", { fw4, zone, family: 4, saddrs, daddrs }) %}
++{%      endfor %}
++{%     endfor %}
++{%    else %}
++		{%+ include("zone-fullcone.uc", { fw4, zone, family: 4, direction: "srcnat", proto: null }) %}
++{%    endif %}
++{%   elif (zone.masq): %}
+ {%    for (let saddrs in zone.masq4_src_subnets): %}
+ {%     for (let daddrs in zone.masq4_dest_subnets): %}
+ 		{%+ include("zone-masq.uc", { fw4, zone, family: 4, saddrs, daddrs }) %}
+ {%     endfor %}
+ {%    endfor %}
+ {%   endif %}
+-{%   if (zone.masq6): %}
++{%   if (zone.masq6 && zone.fullcone6): %}
++{%    if (zone.fullcone_proto && length(zone.fullcone_proto)): %}
++{%     for (let proto in zone.fullcone_proto): %}
++		{%+ include("zone-fullcone.uc", { fw4, zone, family: 6, direction: "srcnat", proto }) %}
++{%     endfor %}
++{%     for (let saddrs in zone.masq6_src_subnets): %}
++{%      for (let daddrs in zone.masq6_dest_subnets): %}
++		{%+ include("zone-masq.uc", { fw4, zone, family: 6, saddrs, daddrs }) %}
++{%      endfor %}
++{%     endfor %}
++{%    else %}
++		{%+ include("zone-fullcone.uc", { fw4, zone, family: 6, direction: "srcnat", proto: null }) %}
++{%    endif %}
++{%   elif (zone.masq6): %}
+ {%    for (let saddrs in zone.masq6_src_subnets): %}
+ {%     for (let daddrs in zone.masq6_dest_subnets): %}
+ 		{%+ include("zone-masq.uc", { fw4, zone, family: 6, saddrs, daddrs }) %}
+--- /dev/null
++++ b/root/usr/share/firewall4/templates/zone-fullcone.uc
+@@ -0,0 +1,33 @@
++{# /usr/share/firewall4/templates/zone-fullcone.uc
++   proto:  string (L4 protocol name) or null
++
++   The srcnat rule carries a port range, because the kernel side is SONiC's
++   and only enforces what fullcone needs when a range is present. SONiC's
++   natmgr never passes --fullcone without one, and get_unique_tuple() checks
++   3-tuple uniqueness only in its NF_NAT_RANGE_PROTO_SPECIFIED branch; with
++   no range, a translated port passes on a 5-tuple check alone. Two inside
++   hosts are then given the same external port, and an unsolicited packet
++   to it goes to whichever of them mapped last.
++
++   nft takes a range only after a match on a protocol that has one - a port,
++   or the icmp id and gre key - so srcnat is split: those protocols with the
++   range, everything else as before. dstnat takes none, like SONiC's DNAT
++   rule; the kernel clears the range there in any case.
++
++   The set is joined here rather than with fw4.set(): that quotes anything
++   that is not an address, and nft does not resolve a quoted protocol name.
++ -#}
++{%
++	let snat_range = "1024-65535";
++	let l4 = (name) => fw4.l4proto(family, { name });
++	let keyed = map([ "tcp", "udp", "sctp", "dccp", "udplite", "icmp", "gre" ], l4);
++	let label = `${zone.name} ${fw4.nfproto(family, true)} fullcone`;
++-%}
++{% if (direction == "srcnat" && !proto): -%}
++meta nfproto {{ fw4.nfproto(family) }} meta l4proto {{ "{ " + join(", ", keyed) + " }" }} fullcone to :{{ snat_range }} comment "!fw4: {{ label }} NAT srcnat"
++		meta nfproto {{ fw4.nfproto(family) }} fullcone comment "!fw4: {{ label }} NAT srcnat, other protocols"
++{% elif (direction == "srcnat" && (l4(proto) in keyed)): -%}
++meta nfproto {{ fw4.nfproto(family) }} meta l4proto {{ l4(proto) }} fullcone to :{{ snat_range }} comment "!fw4: {{ label }} {{ proto }} NAT srcnat"
++{% else -%}
++meta nfproto {{ fw4.nfproto(family) }}{{ proto ? ` meta l4proto ${l4(proto)}` : '' }} fullcone comment "!fw4: {{ label }}{{ proto ? ` ${proto}` : '' }} NAT {{ direction }}"
++{% endif -%}
+--- a/root/usr/share/ucode/fw4.uc
++++ b/root/usr/share/ucode/fw4.uc
+@@ -489,6 +489,29 @@ function nft_try_hw_offload(devices) {
+ 	return (rc == 0);
+ }
+ 
++function nft_try_fullcone() {
++	/* Probe the forms zone-fullcone.uc emits. srcnat carries a port range,
++	 * and a kernel that has the expression but not its range attributes
++	 * should turn fullcone off here rather than fail the whole ruleset. */
++	let nft_test =
++		'add table inet fw4-fullcone-test; ' +
++		'add chain inet fw4-fullcone-test dstnat { ' +
++			'type nat hook prerouting priority -100; policy accept; ' +
++			'fullcone; ' +
++		'}; ' +
++		'add chain inet fw4-fullcone-test srcnat { ' +
++			'type nat hook postrouting priority -100; policy accept; ' +
++			'meta l4proto { tcp, udp } fullcone to :1024-65535; ' +
++			'fullcone; ' +
++		'}; ';
++	let cmd = sprintf("/usr/sbin/nft -c '%s' 2>/dev/null", replace(nft_test, "'", "'\\''"));
++	let ok = system(cmd) == 0;
++	if (!ok) {
++		warn("nft_try_fullcone: fullcone expression not available in kernel\n");
++	}
++	return ok;
++}
++
+ 
+ return {
+ 	read_kernel_version: function() {
+@@ -1475,6 +1498,7 @@ return {
+ 			"dnat",
+ 			"snat",
+ 			"masquerade",
++			"fullcone",
+ 			"accept",
+ 			"reject",
+ 			"drop"
+@@ -1946,6 +1970,7 @@ return {
+ 		}
+ 
+ 		let defs = this.parse_options(data, {
++			fullcone: [ "bool", "0" ],
+ 			input: [ "policy", "drop" ],
+ 			output: [ "policy", "drop" ],
+ 			forward: [ "policy", "drop" ],
+@@ -1980,6 +2005,13 @@ return {
+ 
+ 		delete defs.syn_flood;
+ 
++		if (defs.fullcone) {
++			if (!nft_try_fullcone()) {
++				delete defs.fullcone;
++				warn("fullcone expression not available, disabling fullcone globally\n");
++			}
++		}
++
+ 		this.state.defaults = defs;
+ 	},
+ 
+@@ -2005,6 +2037,9 @@ return {
+ 
+ 			masq6: [ "bool" ],
+ 
++			fullcone: [ "bool" ],
++			fullcone_proto: [ "protocol", null, PARSE_LIST ],
++
+ 			extra: [ "string", null, UNSUPPORTED ],
+ 			extra_src: [ "string", null, UNSUPPORTED ],
+ 			extra_dest: [ "string", null, UNSUPPORTED ],
+@@ -2036,6 +2071,28 @@ return {
+ 			}
+ 		}
+ 
++		/* SONiC fullcone: defaults.fullcone is a global gate.
++		 * When off, all fullcone is disabled regardless of zone config.
++		 * When on, each zone must explicitly set fullcone=1 to enable.
++		 *
++		 * Flatten fullcone_proto (list of protocol objects) into plain
++		 * name strings suitable for direct use in nftables templates.
++		 */
++		zone.fullcone = !!(this.state.defaults.fullcone && zone.fullcone);
++		zone.fullcone6 = zone.fullcone;
++
++		if (zone.fullcone_proto && length(zone.fullcone_proto)) {
++			let names = [];
++			for (let p in zone.fullcone_proto) {
++				if (!p || p.any || !p.name)
++					continue;
++				push(names, p.name);
++			}
++			zone.fullcone_proto = length(names) ? names : null;
++		} else {
++			zone.fullcone_proto = null;
++		}
++
+ 		if (zone.mtu_fix && this.kernel < 0x040a0000) {
+ 			this.warn_section(data, "option 'mtu_fix' requires kernel 4.10 or later");
+ 			return;
+@@ -2205,10 +2262,21 @@ return {
+ 		zone.related_subnets = related_subnets;
+ 		zone.related_physdevs = related_physdevs;
+ 
+-		if (zone.masq || zone.masq6)
++		if (zone.masq) {
+ 			zone.dflags.snat = true;
++			if (zone.fullcone)
++				zone.dflags.dnat = true;
++		}
++
++		if (zone.masq6) {
++			zone.dflags.snat = true;
++			if (zone.fullcone6)
++				zone.dflags.dnat = true;
++		}
+ 
+-		if ((zone.auto_helper && !(zone.masq || zone.masq6)) || length(zone.helper)) {
++		if ((zone.auto_helper && !(zone.masq || zone.masq6 ||
++		     zone.fullcone || zone.fullcone6)) ||
++		    length(zone.helper)) {
+ 			zone.dflags.helper = true;
+ 
+ 			for (let helper in (length(zone.helper) ? zone.helper : this.state.helpers)) {
-- 
2.43.0

